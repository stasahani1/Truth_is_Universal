# Python deps for reextract_response_position.py (remote GPU).
# NOTE: torch is intentionally omitted — use the instance's preinstalled CUDA
# build. If torch is missing, install the CUDA wheel that matches the box first.
transformers
bitsandbytes
accelerate
scikit-learn
scipy
pandas
numpy
matplotlib
tqdm
